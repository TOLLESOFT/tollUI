import { TcPopOverDirective } from './tc-pop-over.directive';

describe('TcPopOverDirective', () => {
  it('should create an instance', () => {
    const directive = new TcPopOverDirective();
    expect(directive).toBeTruthy();
  });
});
